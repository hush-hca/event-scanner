"use client";
import { useEffect, useState } from "react";
type Event={event_id:string;source_timestamp:string;content:string;tickers:string[];classification:{severity?:string;direction?:string;ignored:boolean}};
const tabs=["개요","경보","시장","소스","설정"];
export default function Home(){const [tab,setTab]=useState("개요"),[events,setEvents]=useState<Event[]>([]);useEffect(()=>{fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL??"http://localhost:8000"}/events`).then(x=>x.json()).then(x=>setEvents(x.items??[])).catch(()=>{})},[]);return <main><aside><h1>EventRadar</h1><small>READ-ONLY RISK SCANNER</small>{tabs.map(x=><button className={x===tab?"on":""} onClick={()=>setTab(x)} key={x}>{x}</button>)}</aside><section><header><div><h2>{tab}</h2><p>이벤트 기반 위험 신호를 빠르게 확인하세요.</p></div><b>자동매매 비활성화</b></header>{tab==="개요"&&<div className="cards"><Card t="활성 경보" v={String(events.filter(x=>!x.classification.ignored).length)}/><Card t="고위험" v={String(events.filter(x=>x.classification.severity==="high"||x.classification.severity==="critical").length)}/><Card t="감시 소스" v="1"/></div>}{tab==="시장"?<article><h3>시장 반응 검증</h3><p>가격, 거래량, 호가 깊이, OI 및 펀딩 데이터를 이벤트 전후로 비교합니다.</p></article>:tab==="소스"?<article><h3>소스 신뢰도</h3><p>Binance 공지 · fixture 검증 모드</p><p>공개 시장 데이터 · 연결 대기</p></article>:tab==="설정"?<article><h3>알림 설정</h3><p>Telegram 자격 증명이 설정될 때만 실제 전송됩니다.</p></article>:<Events rows={tab==="개요"?events.filter(x=>!x.classification.ignored):events}/>}</section><style jsx>{`main{min-height:100vh;display:grid;grid-template-columns:230px 1fr;background:#07111f;color:#e9f1fa;font-family:Arial}aside{background:#091522;padding:28px 16px;border-right:1px solid #203149}h1{margin:0}small{display:block;color:#8ea5bd;margin:8px 0 28px}button{display:block;width:100%;text-align:left;border:0;background:transparent;color:#aac0d7;padding:12px;border-radius:7px;margin:5px 0}.on,button:hover{background:#173454;color:white}section{padding:36px;max-width:1250px;width:100%;margin:auto}header{display:flex;justify-content:space-between}h2{margin:0}header p,article{color:#a9bad0}header b{font-size:12px;background:#24384f;padding:9px;border-radius:16px;height:min-content}.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin:24px 0}.card,article,table{background:#0d1b2b;border:1px solid #20344b;border-radius:10px}.card{padding:20px}.card span{color:#91a7c1}.card strong{font-size:32px;display:block;margin-top:10px}article{padding:22px}table{width:100%;border-spacing:0}th,td{text-align:left;padding:14px;border-bottom:1px solid #20344b}th{color:#8ea6c0;font-size:12px}@media(max-width:700px){main{grid-template-columns:1fr}aside{padding:18px}aside button{display:inline-block;width:auto}.cards{grid-template-columns:1fr}section{padding:20px}}`}</style></main>}
function Card({t,v}:{t:string;v:string}){return <div className="card"><span>{t}</span><strong>{v}</strong></div>}
function Events({rows}:{rows:Event[]}){return <article><h3>이벤트 목록</h3>{rows.length?<table><thead><tr><th>시각</th><th>티커</th><th>등급</th><th>방향</th><th>내용</th></tr></thead><tbody>{rows.map(x=><tr key={x.event_id}><td>{new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul"}).format(new Date(x.source_timestamp))}</td><td>{x.tickers.join(",")||"-"}</td><td>{x.classification.ignored?"무시":x.classification.severity}</td><td>{x.classification.direction??"-"}</td><td>{x.content}</td></tr>)}</tbody></table>:<p>표시할 이벤트가 없습니다.</p>}</article>}
